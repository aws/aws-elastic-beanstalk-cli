import os
import subprocess

subprocess.check_call(
    'pytest {} --cov-report term-missing --junitxml=junit-ebcli.xml --cov=ebcli --cov-report xml'.format(
        os.path.join('tests', 'unit')
    ),
    shell=True
)
